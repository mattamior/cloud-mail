# Predecessor Validation Lineage

The predecessor ZeroLocal v0.1 validation is preserved at:

`zerolocal/cloudflare-validation`

The Svif validation line is:

`svif/cloudflare-validation`

The predecessor branch preserves `.chatgpt/` memory, `ZEROLOCAL.yaml`, the ZeroLocal validation workflow, and the original provider-attempt context. Those artifacts are historical evidence only and are not active Svif/Agnir structure.

Production `main` is a separate application line and is not rewritten merely to perform protocol validation.
